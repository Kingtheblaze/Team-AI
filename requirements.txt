# ChronoStream RAG — Python Dependencies
# Core
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.29.0,<1.0
pydantic>=2.5.0,<3.0
pydantic-settings>=2.1.0,<3.0

# Embeddings & ML
sentence-transformers>=2.6.0,<4.0
numpy>=1.26.0,<3.0

# Async HTTP (for Ollama / external LLM calls)
httpx>=0.27.0,<1.0

# Demo UI
streamlit>=1.35.0,<2.0
requests>=2.31.0,<3.0

# Optional: Audio transcription (uncomment if needed)
# faster-whisper>=1.0.0

# Optional: OpenAI integration (uncomment if needed)
# openai>=1.30.0

# Optional: External vector store (uncomment if needed)
# chromadb>=0.5.0
# qdrant-client>=1.9.0

# Dev / Testing
pytest>=8.0.0,<9.0
pytest-asyncio>=0.23.0,<1.0
